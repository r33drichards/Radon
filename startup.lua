-- startup.lua -- auto-runs the Radon shop whenever the turtle boots or its
-- chunk reloads. Drop this on the turtle alongside radon.lua and the shop
-- comes back on its own after reboots / chunk reloads (no manual `radon`).
shell.run("radon")
